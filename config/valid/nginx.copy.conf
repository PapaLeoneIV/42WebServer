server {
  listen 8001;                                                                    # listening port, mandatory parameter
  host localhost;                                                                 # host or 127.0.0.1 by default
  server_name test;                                                             # specify server_name, need to be added into /etc/hosts to work
  error_page 404 /home/papaleoneiv/Desktop/42WebServer/static/errorPage/404.html; # custom error page
  error_page 405 /home/papaleoneiv/Desktop/42WebServer/static/errorPage/405.html; # custom error page
  error_page 500 /home/papaleoneiv/Desktop/42WebServer/static/errorPage/500.html; # custom error page

  client_max_body_size 1024;          # max request body size in bytes
  root /home/papaleoneiv/Desktop/;    # root folder of site directory, full or relative path, mandatory parameter
  index index.html;                   # default page when requesting a directory, index.html by default

  location /tours {                   
      root /home/papaleoneiv/Desktop/;   # root folder of the location, if not specified, taken from the server. 
      autoindex on;                      # turn on/off directory listing
      allow_methods POST GET;            # allowed methods in location, GET only by default
      index index.html;                  # default page when requesting a directory, copies root index by default, remove it to enable directory listing
      alias  42WebServer/;               # replaces location part of URI. 
  }

  location / {
      allow_methods POST GET;         # allowed methods in location, GET only by default
      index index.html;               # default page when requesting a directory, copies root index by default, remove it to enable directory listing
  }

  location /cgi-bin {
      root ./;                                                 # cgi-bin location, mandatory parameter
      cgi_path /usr/bin/python3 /bin/bash;                     # location of interpreters installed on the current system, mandatory parameter
      cgi_ext .py .sh;                                         # extensions for executable files, mandatory parameter
  }
}
server {
  listen 8002;                                                                    # listening port, mandatory parameter
  host localhost;                                                                 # host or 127.0.0.1 by default
  server_name my-app;                                                             # specify server_name, need to be added into /etc/hosts to work

  client_max_body_size 1024;          # max request body size in bytes
  root /home/papaleoneiv/Desktop/;    # root folder of site directory, full or relative path, mandatory parameter
  index index.html;                   # default page when requesting a directory, index.html by default

  location /tours {
      root /home/papaleoneiv/Desktop/;   # root folder of the location, if not specified, taken from the server.
      autoindex on;                      # turn on/off directory listing
      allow_methods POST GET;            # allowed methods in location, GET only by default
      index index.html;                  # default page when requesting a directory, copies root index by default, remove it to enable directory listing
      alias  42WebServer/;               # replaces location part of URI.
  }

  location / {
      allow_methods POST GET;         # allowed methods in location, GET only by default
      index index.html;               # default page when requesting a directory, copies root index by default, remove it to enable directory listing
  }

  location /cgi-bin {
      root ./;                                                 # cgi-bin location, mandatory parameter
      cgi_path /usr/bin/python3 /bin/bash;                     # location of interpreters installed on the current system, mandatory parameter
      cgi_ext .py .sh;                                         # extensions for executable files, mandatory parameter
  }
}
